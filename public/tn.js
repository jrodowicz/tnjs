// selector code
window.$ = function(selector) {
    return document.querySelectorAll(selector);              // The returns everything with attributes matching elements
}

// event code

// animation code

// ajax code
